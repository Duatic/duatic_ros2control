safety_scaling_controller:
  joints:
    type: string_array
    default_value: []
    description: Names of joints used by the controller. If this is left empty all safety scaling interfaces are claimed
